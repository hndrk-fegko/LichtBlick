# LichtBlick - Environment Variables

# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
# =====================
# DB_TYPE: mysql, sqlite, or none
#   - none: Skip database initialization (for npm install on shared hosting where better-sqlite3 can't compile)
#   - mysql: Use MySQL/MariaDB (requires DB_HOST, DB_USER, DB_PASSWORD, DB_NAME)
#   - sqlite: Use SQLite (default, uses DB_PATH)
# If DB_HOST is set, MySQL is used automatically (even without DB_TYPE=mysql)
# If nothing is set, SQLite is used as fallback

DB_TYPE=
# DB_TYPE=sqlite
# DB_TYPE=mysql
# DB_TYPE=none

# SQLite Configuration (used when DB_TYPE=sqlite or default)
DB_PATH=../data/lichtblick.db

# MySQL/MariaDB Configuration (used when DB_TYPE=mysql or DB_HOST is set)
DB_HOST=
# DB_HOST=localhost
DB_PORT=3306
DB_USER=
# DB_USER=lichtblick
DB_PASSWORD=
# DB_PASSWORD=your_secure_password
DB_NAME=lichtblick

# Logging
LOG_LEVEL=info
LOG_FILE_PATH=../logs

# Security
ADMIN_PIN=1234

# File Upload
UPLOAD_DIR=../data/uploads
MAX_FILE_SIZE=10485760

# CORS (optional)
CORS_ORIGIN=*
